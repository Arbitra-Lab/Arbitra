apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: arbitra-backend-pdb
  namespace: production
  labels:
    app: arbitra-backend
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: arbitra-backend
  unhealthyPodEvictionPolicy: AlwaysAllow
